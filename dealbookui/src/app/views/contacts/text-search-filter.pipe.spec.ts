import { TextSearchFilterPipe } from './text-search-filter.pipe';

describe('TextSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TextSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
